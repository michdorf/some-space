import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.michdorf.somespace',
  appName: 'some-space',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
